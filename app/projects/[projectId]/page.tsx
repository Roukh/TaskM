export const metadata = { title: 'Project' };

export default function ProjectPage() {
  return (
    <div className="flex h-screen items-center justify-center text-muted-foreground">
      Canvas coming in Phase 1
    </div>
  );
}
